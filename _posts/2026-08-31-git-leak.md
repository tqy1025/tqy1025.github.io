---
layout: post
title: "Git 泄漏漏洞"
date: 2026-08-31 08:00:00 +0800
categories: [Web-security]
tags: ["Git 泄漏"]
---

## 原理

Git 泄漏是指 `.git` 目录被意外暴露在生产环境中，攻击者可以通过访问 Git 元数据文件（如 `.git/HEAD`、`.git/config`、`.git/objects/*` 等）重建完整的源代码仓库。

`.git` 目录保存了 Git 仓库的版本管理信息，包括：

- 当前分支信息
- 提交记录
- 文件对象
- 历史版本
- 远程仓库配置
- 已经删除但仍存在于历史提交中的文件

常见结构如下：

```text
.git/
├── HEAD
├── config
├── index
├── refs/
│   ├── heads/
│   └── remotes/
├── logs/
└── objects/
    ├── info/
    ├── pack/
    └── xx/
```

其中：

- `.git/HEAD` 指向当前分支。
- `.git/index` 保存暂存区中的文件路径信息。
- `.git/refs` 保存分支和标签引用。
- `.git/logs` 可能泄漏提交记录和提交者信息。
- `.git/objects` 保存提交、目录树和文件内容。
- `.git/config` 可能泄漏远程仓库地址及内部配置信息。

## 检测方法

1. 访问 `/.git/`：返回 `403 Forbidden`，说明目录可能存在，但禁止直接浏览。
2. 访问 `/.git/HEAD`：返回 `ref: refs/heads/master` 或 `ref: refs/heads/main`，说明 Git 元数据可以读取。
3. 访问 `/.git/config`：返回 `[core]`、`[remote "origin"]` 等配置，基本可以确认存在 Git 泄漏。

```bash
curl -i https://example.com/.git/
curl -i https://example.com/.git/HEAD
curl -i https://example.com/.git/config
```

## 利用工具

### git-dumper

`git-dumper` 可以从允许访问部分 `.git` 文件的网站中恢复 Git 仓库。

项目地址：[arthaud/git-dumper](https://github.com/arthaud/git-dumper)

基本用法：

```bash
git-dumper https://example.com/.git/ recovered-repository
```


### GitTools

GitTools 包含用于检测、下载和提取 Git 仓库的脚本。

项目地址：[internetwache/GitTools](https://github.com/internetwache/GitTools)


使用 Dumper 下载泄漏的仓库：

```bash
bash Dumper/gitdumper.sh \
  https://example.com/.git/ \
  recovered-repository
```

使用 Extractor 提取各个提交中的文件：

```bash
bash Extractor/extractor.sh \
  recovered-repository \
  recovered-files
```


## 防御措施

### 部署时排除 `.git`

不要直接把项目根目录完整复制到生产服务器。

错误示例：

```bash
cp -r project/ /var/www/html/
```

更安全的做法是只部署构建结果：

```bash
cp -r dist/ /var/www/html/
```

或者使用打包命令排除 `.git`：

```bash
rsync -av \
  --exclude=".git" \
  --exclude=".env" \
  project/ /var/www/html/
```

### Nginx 禁止访问

在 Nginx 配置中加入：

```nginx
location ~* /\.git(?:/|$) {
    return 404;
}
```

修改后检查配置：

```bash
nginx -t
```

重新加载：

```bash
sudo systemctl reload nginx
```

### Apache 禁止访问

可以在 Apache 配置中加入：

```apache
RedirectMatch 404 /\.git
```

或者：

```apache
<DirectoryMatch "/\.git">
    Require all denied
</DirectoryMatch>
```

重新加载 Apache：

```bash
sudo systemctl reload apache2
```

### 删除服务器上的 `.git`

如果生产目录不需要 Git，可以删除部署目录中的 `.git`：

```bash
rm -rf /var/www/html/.git
```

执行前必须确认目标路径准确，避免误删开发仓库。

只删除 `.git` 目录并不能消除已经发生的泄漏。如果仓库中出现过密码、令牌或密钥，还必须立即进行吊销和轮换。

### 使用 CI/CD 部署构建产物

推荐流程：

```text
代码仓库
   ↓
自动构建
   ↓
生成 dist、build 或 public
   ↓
仅将构建产物部署到服务器
```

生产环境不应包含：

```text
.git/
.github/
.env
tests/
开发脚本
本地配置
```

### 避免提交敏感信息

将敏感文件加入 `.gitignore`：

```gitignore
.env
.env.*
config.local.*
*.pem
*.key
secrets/
```

但需要注意，`.gitignore` 只对尚未被 Git 跟踪的文件有效。已经提交过的文件不会因为加入 `.gitignore` 而自动从历史记录中消失。

应该使用环境变量或专门的密钥管理服务保存：

- 数据库密码
- API Token
- SSH 私钥
- 云服务密钥
- JWT 签名密钥

### 泄漏后的应急处理

发现 Git 泄漏后，应当：

1. 立即阻止外部访问 `.git`。
2. 检查服务器访问日志，确定是否有人下载过相关文件。
3. 吊销并轮换仓库中出现过的密码、Token 和密钥。
4. 删除生产服务器上的 `.git` 目录。
5. 检查 Git 的全部历史提交，而不只是当前版本。
6. 必要时清理 Git 历史并重新发布仓库。
7. 检查泄漏的账号是否存在异常登录或权限滥用。
8. 修正部署流程，防止再次将版本库元数据上传。

## CTF 真题

### ctf2 → N1BOOK → 粗心的小李
